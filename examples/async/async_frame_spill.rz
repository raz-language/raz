// Copyright 2026 Mario Vinciguerra
// SPDX-License-Identifier: Apache-2.0

async fn compute() -> i64 {
    return 2;
}

async fn pipeline() -> i64 {
    i64 before = 40;
    i64 child = spawn compute();
    i64 ready = await child;

    return before + ready;
}

fn main() -> i64 {
    return 0;
}
