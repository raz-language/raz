// Copyright 2026 Mario Vinciguerra
// SPDX-License-Identifier: Apache-2.0

fn main() -> i64 {
    i64 amount = 40;

    auto once = move fn(i64 value) -> i64 {
        return value + amount;
    };

    auto duplicate = clone(once);

    return duplicate(2);
}
