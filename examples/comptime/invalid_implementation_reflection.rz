// Copyright 2026 Mario Vinciguerra
// SPDX-License-Identifier: Apache-2.0

trait Storage {
    type Key;

    fn get(Self& self) -> i64;
}

struct Buffer {
    i64 value;
}

comptime {
    assert(impl_associated_type_count<Buffer, Storage>() == 0);
}

fn main() -> i64 {
    return 0;
}
