// Copyright 2026 Mario Vinciguerra
// SPDX-License-Identifier: Apache-2.0

async fn child() -> i64 {
    return 2;
}

async fn invalid() -> i64 {
    i64 local = 40;
    i64& view = &local;
    i64 task = spawn child();
    i64 ready = await task;

    return *view + ready;
}
