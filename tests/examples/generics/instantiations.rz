// Copyright 2026 Mario Vinciguerra
// SPDX-License-Identifier: Apache-2.0


enum Option<T> {
    None,
    Some(T),
}

enum Result<T, E> {
    Ok(T),
    Error(E),
}

struct Pair<T, U> {
    T first;
    U second;
}

fn main() {
    Option<i64> optional;
    Result<i64, string> result;
    Pair<i64, string> pair;
}
