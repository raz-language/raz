// Copyright 2026 Mario Vinciguerra
// SPDX-License-Identifier: Apache-2.0

enum Result<T, E> {
    Ok(T),
    Error(E),
}

fn produce() -> Result<i64, i64> {
    return Result<i64, i64>::Ok(1);
}

fn invalid() -> i64 {
    return produce()?;
}
