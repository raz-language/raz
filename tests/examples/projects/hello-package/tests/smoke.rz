// Copyright 2026 Mario Vinciguerra
// SPDX-License-Identifier: Apache-2.0


// `raz test` discovers `test_` functions. A `main` here would collide with
// the package's own entry point, which is compiled alongside the tests.
fn test_smoke() -> i64 {
    return 0;
}
