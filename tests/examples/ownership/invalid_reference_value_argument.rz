// Copyright 2026 Mario Vinciguerra
// SPDX-License-Identifier: Apache-2.0

fn consume(u64 value) -> u64 {
    return value;
}

fn invalid(u64&mut counter) -> u64 {
    // A reference is not a scalar value. The caller must write
    // `consume(*counter)` when it intends to pass the referent.
    return consume(counter);
}
