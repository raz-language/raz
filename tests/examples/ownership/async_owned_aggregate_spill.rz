// Copyright 2026 Mario Vinciguerra
// SPDX-License-Identifier: Apache-2.0


struct Resource {
    i64 handle;
}

impl Drop for Resource {
    fn drop(Resource&mut self) {
        self;
    }
}

struct Bundle {
    Resource first;
    Resource second;
}

async fn child() -> i64 {
    return 1;
}

async fn pipeline() -> i64 {
    Bundle bundle = Bundle {
        first: Resource {
            handle: 20
        },
        second: Resource {
            handle: 22
        }
    };

    i64 task = spawn child();
    i64 ready = await task;

    return bundle.first.handle + bundle.second.handle + ready;
}

fn main() -> i64 {
    return 0;
}
