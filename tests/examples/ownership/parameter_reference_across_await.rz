// Copyright 2026 Mario Vinciguerra
// SPDX-License-Identifier: Apache-2.0


async fn child() -> i64 {
    return 2;
}

async fn valid(i64& input) -> i64 {
    i64 task = spawn child();
    i64 ready = await task;

    return *input + ready;
}
