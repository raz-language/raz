// Copyright 2026 Mario Vinciguerra
// SPDX-License-Identifier: Apache-2.0

// External mutable data symbols are represented in shared HIR/MIR.
// A native link must provide host_counter.
extern global mut i64 host_counter;

fn increment_host_counter() -> i64 {
    host_counter += 1;
    return host_counter;
}
