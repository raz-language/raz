// Copyright 2026 Mario Vinciguerra
// SPDX-License-Identifier: Apache-2.0


// Shared module storage must behave the same through Forge and LLVM.
global mut i64 counter = 40;
static i64 increment = 2;

fn bump() -> i64 {
    counter += increment;
    return counter;
}

fn main() -> i64 {
    return bump();
}
