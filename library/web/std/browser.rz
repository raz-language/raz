// Copyright 2026 Mario Vinciguerra
// SPDX-License-Identifier: Apache-2.0

namespace web::std::browser;

/// Marker for the browser standard library. Browser-facing APIs live below
/// web::std and are intentionally independent from the native `std` package.
public fn available() -> bool {
    return true;
}
