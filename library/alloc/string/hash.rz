// Copyright 2026 Mario Vinciguerra
// SPDX-License-Identifier: Apache-2.0


namespace alloc::string::hash;


import core::hash;
import alloc::string;

extern fn raz_rt_hash_bytes(usize data, i64 length) -> i64;

impl Hash for String {
    fn hash(String& self) -> i64 {
        return raz_rt_hash_bytes(self.data, self.length);
    }
}

impl Eq for String {
    fn equals(String& self, String& other) -> bool {
        if (self.length != other.length) {
            return false;
        }
        return raz_rt_bytes_equal(self.data, other.data, self.length) == 1;
    }
}
