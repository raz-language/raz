# Performance Baselines

Store intentionally promoted, hardware/toolchain-specific benchmark snapshots here when a CI runner or release machine has been designated as a stable performance reference.

Keep compiler and runtime snapshots separate (`compiler.json` and `runtime.json`). Do not manufacture a baseline from the same run being qualified. See `docs/PERFORMANCE-QUALIFICATION.md` for threshold and promotion policy.
